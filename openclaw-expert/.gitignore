# Lazy-fetched upstream doc snapshots — regenerate via scripts/fetch-page.sh
cache/*.md
cache/*.meta.json
cache/*.html-bad-*
!cache/.gitkeep

# Written by refresh-catalog.sh before every regen — churns on every refresh
catalog.json.bak
